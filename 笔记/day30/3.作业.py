#!/usr/bin/env python
# -*- coding:utf-8 -*-
# import time
# for i in range(0,101,2):
#      time.sleep(0.1)
#      char_num = i//2      #打印多少个'*'
#      per_str = '\r%s%% : %s\n' % (i, '*' * char_num) if i == 100 else '\r%s%% : %s'%(i,'*'*char_num)
#      print(per_str,end='', flush=True)
# import sys
# def processBar(num, total):
#     rate = num / total
#     rate_num = int(rate * 100)
#     if rate_num == 100:
#         r = '\r%s>%d%%\n' % ('=' * rate_num, rate_num,)
#     else:
#         r = '\r%s>%d%%' % ('=' * rate_num, rate_num,)
#     sys.stdout.write(r)
#     sys.stdout.flush
#
# processBar(2738828729,10100019909)


# 1.通读网络编程的博客
# 2.整理这四天的知识点和默写
# 3.计算器
# 4.讲解计算器
# 5.网络编程的前三个需求
